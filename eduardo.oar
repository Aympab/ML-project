#!/bin/bash

### nombre de cores et duree max du job   -p host='node that you'll want'
#OAR -l core=28,walltime=60:00:00
#OAR -p host='n-in80'
## -l core=80,walltime=00:00:30 -S ./run.oar

### -O name of log and -E name of log error
#OAR -n GridSearchML
#OAR -O %jobid%.log
#OAR -E %jobid%.log

python EduardoTrain.py

echo "End computation"
